---
interface Props {
  quote: string;
  name: string;
  date: string;
  location?: string;
  original?: string;
  variant?: 'card' | 'plain';
}

const { quote, name, date, location = '', original = '', variant = 'card' } = Astro.props;
const attribution = [name, location, date].filter(Boolean).join(' · ');

const wrapClass =
  variant === 'card'
    ? 'rounded-2xl border border-[var(--color-hairline)] dark:border-[var(--color-stone-grey)] bg-white dark:bg-[var(--color-schist-ink)] p-5'
    : '';
---

<figure class={wrapClass}>
  <div class="text-[var(--color-sun-gold-dark)] text-sm mb-2" aria-hidden="true">★★★★★</div>
  {original && (
    <blockquote lang="und" class="text-sm leading-relaxed mb-2">“{original}”</blockquote>
  )}
  <blockquote class:list={['text-sm leading-relaxed text-[var(--color-stone-grey)] dark:text-[var(--color-stone)]', original && 'italic']}>
    {original ? quote : `“${quote}”`}
  </blockquote>
  <figcaption class="mt-3 text-xs font-semibold text-[var(--color-graphite)] dark:text-white">{attribution}</figcaption>
</figure>
